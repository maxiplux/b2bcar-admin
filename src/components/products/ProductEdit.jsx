import React, { useState,useEffect, useDebugValue } from 'react';

const ProductEdit = (props)=> { 
  const [dataSource, setDataSource] = useState([]);
  return (<>Estoy Editando , luego termino</>)
}

export default ProductEdit;
